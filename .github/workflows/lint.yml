name: Lint

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  changes:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      powershell: ${{ steps.filter.outputs.powershell }}
      markdown: ${{ steps.filter.outputs.markdown }}
      yaml: ${{ steps.filter.outputs.yaml }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: dorny/paths-filter@d1c1ffe0248fe513906c8e24db8ea791d46f8590 # v3
        id: filter
        with:
          filters: |
            powershell:
              - '**/*.ps1'
              - 'PSScriptAnalyzerSettings.psd1'
            markdown:
              - '**/*.md'
              - '.markdownlint.*'
              - '.markdownlint.jsonc'
            yaml:
              - '**/*.yml'
              - '**/*.yaml'
              - '.yamllint.yml'

  powershell:
    needs: changes
    if: always() && (needs.changes.result == 'skipped' || needs.changes.outputs.powershell == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Run PSScriptAnalyzer
        shell: pwsh
        run: |
          Install-Module -Name PSScriptAnalyzer -RequiredVersion 1.25.0 -Scope CurrentUser -Force
          $results = Invoke-ScriptAnalyzer -Path . -Recurse -Settings ./PSScriptAnalyzerSettings.psd1
          if ($results) {
            $results | Format-Table RuleName, Severity, ScriptName, Line, Message -AutoSize
            throw "PSScriptAnalyzer found $(@($results).Count) issue(s)."
          }
          Write-Host "PSScriptAnalyzer: all checks passed."

  markdown:
    needs: changes
    if: always() && (needs.changes.result == 'skipped' || needs.changes.outputs.markdown == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - run: npx markdownlint-cli2@0.17.2 --config .markdownlint.json '**/*.md'

  yaml:
    needs: changes
    if: always() && (needs.changes.result == 'skipped' || needs.changes.outputs.yaml == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - run: pip install yamllint==1.38.0
      - run: yamllint -c .yamllint.yml .
